{
  "name": "whisper-cpp-node-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "copy-binary": "node ./scripts/copy-binary.js",
    "prepublish": "pnpm copy-binary && pnpm build"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
