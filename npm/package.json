{
  "name": "whisper-cpp-node-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "copy-binary": "./scripts/copy-binary.sh",
    "prepublish": "pnpm copy-binary && pnpm build"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
