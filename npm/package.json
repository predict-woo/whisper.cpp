{
  "name": "whisper-cpp-node-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "copy-binary": "node ./scripts/copy-binary.js",
    "prepublish-all": "pnpm copy-binary && pnpm build",
    "release": "pnpm prepublish-all && pnpm -r publish --access public"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
